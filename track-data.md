The clean mainline is now identified: the relation contains 18 “forward” ways totaling 10.342 km, matching the published 10.3 km loop. The remaining members are ramps/connectors, so I’ll keep them as a separate layer instead of accidentally folding them into the race track. Nearby buildings are being clipped to a 150 m corridor around the mainline.

It contains:

Closed clockwise route centerline: 10,341.932 m
GeoJSON route segments, including separate ramps/connectors
7,223 building footprints within 150 m
Local metre-based JSON and spline CSV
OBJ/MTL building massing and flat 10 m road preview
WGS84 source data, metadata and import instructions

The extraction uses OpenStreetMap relation 6085197, identified through Wikidata. The published route length is 10.3 km and it operates clockwise
